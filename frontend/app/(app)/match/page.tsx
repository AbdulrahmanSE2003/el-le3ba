import StartMatch from "@/features/match/components/StartMatch";
const page = () => {
  return (
    <section
      className={`h-fullflex justify-center items-center text-background dark:text-foreground`}
    >
      <StartMatch />
    </section>
  );
};

export default page;
