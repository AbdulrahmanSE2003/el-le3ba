info:
  name: Signup
  type: http
  seq: 2

http:
  method: POST
  url: "{{URL}}/users/signup"
  body:
    type: json
    data: |-
      {
        "name":"admin",
        "email":"tadmin@gmail.com",
        "password":"pass1234",
        "passwordConfirm":"pass1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
