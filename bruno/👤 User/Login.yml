info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{URL}}/users/login"
  body:
    type: json
    data: |-
      {
        "email":"abdulrahman.saad2303@gmail.com",
        "password":"pass1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
